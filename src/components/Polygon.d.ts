import PolygonProps from './PolygonProps';
export default function Polygon({ points, visible, enabled, selected, onSelect, onDeselect, lineDash, lineDashOffset, lineJoin, lineWidth, strokeColor, strokeOpacity, strokeStart, strokeEnd, fillColor, fillOpacity, fillRule, }: PolygonProps): null;
